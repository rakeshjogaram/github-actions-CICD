---
name: hello Workflow
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "hello Rakesh jogaram"
      - name: say hello the username
        run: echo "hello $USER"
